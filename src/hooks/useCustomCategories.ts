// Deprecated — use useCategories from '../hooks/useCategories' instead.
// Kept as a no-op shim to avoid orphan import errors during migration.
export { useCategories as useCustomCategories } from './useCategories';
export type { CustomCategory } from './useCategories';
